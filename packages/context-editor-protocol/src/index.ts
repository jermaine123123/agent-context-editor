/**
 * Neutral protocol facade. The implementation currently lives beside the
 * Pi Desktop core so the 0.1.4 adapter keeps one source of truth while the
 * second-host adapter can import a host-independent contract.
 */
/** @deprecated Import from @context-editor/core instead. */
export * from '../../context-editor-core/src/protocol.js'
