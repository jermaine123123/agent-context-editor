{
  "$schema": "pi-desktop-adapter/v1",
  "id": "pi-context-editor",
  "displayName": "Pi Context Editor",
  "description": "Inspect the active context with visual-only hide, restore, search, and unit controls.",
  "match": {
    "names": ["pi-context-editor"],
    "commands": ["/ctx"]
  },
  "tier": "native",
  "slash": {
    "/ctx": "execute"
  }
}
